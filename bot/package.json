{
  "name": "morpho-flash-arb-bot",
  "version": "0.2.0",
  "private": true,
  "description": "Off-chain scanner that finds liquidatable Moonwell positions on Base, prices the DEX-side exit on Aerodrome, and fires FlashLoanArbitrage.executeArbitrage when the route is profitable. Now with ML-enhanced predictions.",
  "type": "module",
  "scripts": {
    "scan": "tsx scanBorrowers.ts",
    "check": "tsx checkPosition.ts",
    "position": "tsx getPosition.ts",
    "watch": "tsx watch.ts",
    "ml-watch": "tsx ml-enhanced-watch.ts",
    "preview": "tsx server.ts",
    "train-models": "python3 ml/train_models.py",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "ethers": "^6.17.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.10",
    "typescript": "^7.0.2"
  }
}
